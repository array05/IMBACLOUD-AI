WORKER_TOKEN=auto-generated-on-install
WORKER_WORKSPACE=/opt/client/project
WORKER_PORT=9090
SHELL_TIMEOUT=120

WORKER_ID=client-prod
WORKER_PUBLIC_URL=http://YOUR_PUBLIC_IP:9090
GATEWAY_URL=http://31.129.101.206:8080
REGISTRATION_TOKEN=imbai-reg-secret-2026
HEARTBEAT_INTERVAL=30

# Prefer native (runs on host OS, not Docker):
#   ./scripts/install-worker.sh --worker-id client-prod --workspace /root
#
# Docker mode (isolated container — hostname will be container id):
#   ./scripts/install-worker.sh --worker-id client-prod --workspace /root --docker
